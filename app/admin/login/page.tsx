interface LoginPageProps { }

const LoginPage: React.FC<LoginPageProps> = ({ }) => {
    return (
        <div className="h-full w-full flex items-center justify-center bg-cover bg-center bg-no-repeat">
            Login Page
        </div>
    );
};

export default LoginPage;
